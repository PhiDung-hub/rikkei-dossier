# Dependencies & build output
node_modules/
dist/
.vite/

# Raw source — not committed. The built public/data.gz is what ships.
rikkei-managers.csv
managers.clean.json

# public/images holds admin-uploaded photos (committed by the admin Worker) — keep them

# Misc
.DS_Store
*.log
.playwright-mcp/
